rectangle = fn(width, height) -> {
	type.assert(width, "int");
	type.assert(height, "int");
	width = width;
	height = height;
	__type = "rectangle";
};

my_rect = rectangle(10, 10);
println(type.of(my_rect));
